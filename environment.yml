name: bnd
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - pip
  - python=3.10
  - numpy
  - pandas
  - pydantic
  - pynwb
  - scipy
  - pytest
  - rich
  - typer
  - python-dotenv
  - pytorch
  - pytorch-cuda=11.8
  - pip:
      - -e ./
      - neuroconv==0.6.3
      - docker
      - ndx-pose
      - probeinterface
      - spikeinterface
      - ruamel-yaml
      - kilosort
